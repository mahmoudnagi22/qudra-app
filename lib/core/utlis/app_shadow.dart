import 'package:flutter/material.dart';

class AppShadows {
  static const BoxShadow dropShadow = BoxShadow(
    color: Color(0x43414140),
    offset: Offset(0, 0),
    blurRadius: 2,
    spreadRadius: 0,
  );
}
